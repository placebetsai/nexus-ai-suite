# Stub sweep - 2026-09-26

## Verdict
Both apps are substantially real. fashionistas.ai uses real AI vision (llama-3.2-11b), real D1 database, real R2 image storage, real KV caching, and a verified fee engine with cited sources. The only faked path is the marketplace checkout, explicitly labelled as demo. createstuff-marketing's landing page is static marketing; the builder (app.js + createstuff-api) runs a real 3-stage AI pipeline (planner → writer → verifier/repair) with Hive relay failover, real GitHub import via user PAT, real in-browser ZIP generation, and a real per-project app backend. The shim layer in app.js replaces missing forge-api endpoints with real alternatives (local catalog, forge-api generate/publish, browser ZIP, GitHub API). No feature claims to work when it only shows a toast.

## Real (verified)

| feature | where | what proves it is real |
|---------|-------|------------------------|
| AI photo analysis (vision) | workers/fashionistas-api/src/index.js:974-986 | Calls `env.AI.run("@cf/meta/llama-3.2-11b-vision-instruct")` with base64 image, parses JSON response with confidence score |
| AI title optimizer | workers/fashionistas-api/src/index.js:998-1004 | Calls `env.AI.run("@cf/meta/llama-3.2-3b-instruct")` with structured prompt, returns optimized title + keywords |
| Fee engine with verified sources | workers/fashionistas-api/src/index.js:361-488 | 17 platforms with `verified:true/false`, `source` URLs citing platform help pages (e.g. Depop company news 2024-07-18, Vinted US price list 2026) |
| Shipping estimate (USPS/Pirate Ship) | workers/fashionistas-api/src/index.js:1046-1057 | Calculates from weight_oz using published rate tables; Pirate Ship gets 20% discount |
| Listings CRUD (create/read/update/delete) | workers/fashionistas-api/src/index.js:1071-1238 | D1 INSERT/SELECT/UPDATE/DELETE with validation, COALESCE for partial updates |
| Orders & profit analytics | workers/fashionistas-api/src/index.js:1305-1371 | Aggregates from orders table: SUM(sale_price), SUM(fee), SUM(profit), GROUP BY platform/day |
| Direct messages (blocking, threads) | workers/fashionistas-api/src/index.js:1431-1543 | Creates dm_messages/dm_blocks tables on demand; blocking is bidirectional; unread counts via SQL |
| Pop-up stores with geocoding | workers/fashionistas-api/src/index.js:252-275, 1387-1429 | Creates popup_stores table; uses Nominatim (OpenStreetMap) for geocode; haversine distance in JS |
| Hive relay async chat | workers/fashionistas-api/src/index.js:596-641 | POST to HIVE_URL with async=true, polls job endpoint until done/failed/timeout |
| 3-stage AI build pipeline | workers/createstuff-api/src/index.js:556-1301 | Planner (planAgent) → Writer (gen with CODE_SYS) → Verifier (qualityFlags) → Repair (repairAgent) |
| GitHub import (user PAT) | workers/createstuff-api/src/index.js:1773-1846 | Fetches repo tree/contents via api.github.com with user's Bearer token; saves files to D1 |
| In-browser ZIP generation | apps/createstuff-marketing/app.js:76-121 | Store-method ZIP writer with CRC32, builds Blob from project files fetched from /published/ |
| Per-project app backend | workers/createstuff-api/src/index.js:1303-1609 | /app/:id/api/:collection CRUD with app_data/app_users/app_sessions tables, scoped by project_id |
| Asset sanitization (external images → CSS gradients) | workers/createstuff-api/src/index.js:471-509 | Replaces external img src/background-image with deterministic gradient panels |
| Shim layer for missing forge-api routes | apps/createstuff-marketing/app.js:40-311 | /api/templates → local catalog; /api/builds → forge-api generate/publish; /api/github/* → api.github.com with PAT |
| A→Z launch path with live evidence | apps/createstuff-marketing/app.js:1281-1771 | Each step makes real fetch(), prints HTTP status, ms, bytes, response body; stops on first failure |
| Camera capture + base64 encode | apps/fashionistas/index.html:2394-2411 | getUserMedia → canvas.drawImage → toDataURL → analyzeImage calls /api/ai/analyze |
| Leaflet map + OpenStreetMap tiles | apps/fashionistas/index.html:1336-1499 | Loads Leaflet from unpkg, tile.openstreetmap.org, Nominatim geocoding, real markers |
| CSV export (inventory/orders) | workers/fashionistas-api/src/index.js:1615-1625 | SELECT from D1, builds CSV with proper escaping, Content-Disposition attachment |
| Quality scoring for listings | workers/fashionistas-api/src/index.js:1266-1291 | Base 50 + weighted checks (photo +15, title>5 +10, desc>80 +10, price +8, size+cond +7) capped at 100 |

## Legitimate demo

| feature | where | how it is labelled |
|---------|-------|-------------------|
| Marketplace checkout (buy button) | workers/fashionistas-api/src/index.js:1118-1138 | Returns `{demo: true, message: "Demo checkout — no money moved. Stripe and PayPal are not live yet."}` |
| Demo seller login | apps/fashionistas/index.html:1245, 1067 | Button says "Log in as demo seller", fills username="demo", password="Primetime2026!" |
| Sample jacket photo for trial | apps/fashionistas/index.html:3289-3305, 3816-3828 | `useSample()` loads "sample-jacket.jpg" explicitly for demo; `hcSampleB64()` caches it |
| Guide A→Z tour sample data | apps/fashionistas/index.html:3704-3711 | `hcPickListing()` returns `{fake:true, unsaved:true}` or `{fake:true}` when no real listing exists |
| Hive status display in dashboard | apps/createstuff-marketing/app.js:542-549 | Static agent list with icons; no claim of live status — UI element showing the agent team concept |
| Local template catalog | apps/createstuff-marketing/app.js:60-73 | `CS_TEMPLATE_CATALOG` constant; shim serves it for `/api/templates` — explicitly a local fallback |

## Stubs found

| # | feature | file:line | what it fakes | how you proved it |
|---|---------|-----------|---------------|-------------------|
| 1 | AI analyze fallback | workers/fashionistas-api/src/index.js:986 | Returns hardcoded `{type:"Clothing Item",priceMin:25,priceMax:75,condition:"Good",category:"Tops",color:"Various",brand:"Unknown",sizeHint:"",confidence:60}` when vision model response fails to parse | Line 984-986: `const parsed = aiJson(text, null); if (parsed) return json({source:"ai",...parsed}); return json({source:"ai", note:"description: "+..., confidence:60, type:"Clothing Item", priceMin:25, priceMax:75, condition:"Good", category:"Tops", color:"Various", brand:"Unknown", sizeHint:""})` — the fallback is a fixed object |
| 2 | Title optimizer fallback | workers/fashionistas-api/src/index.js:1003-1004 | Returns `{optimized: title, keywords: [], tips: []}` when AI response fails to parse | Line 1003: `const parsed = aiJson(t, null) || { optimized: title, keywords: [], tips: [] };` — empty arrays, echoes original title |
| 3 | Message templates fallback | workers/fashionistas-api/src/index.js:1376-1377 | Returns 6 hardcoded templates when AI call fails | Line 1377: `return json({templates: lines.length ? lines.slice(0,6) : ["Welcome: Thanks for your order!", "Shipped: Your item shipped today", "Thank you: Appreciate your purchase!", "Bundle: Bundle more items and save", "Offer: Happy to consider fair offers", "Returns: See our return policy"]})` |
| 4 | Sample jacket image reference | apps/fashionistas/index.html:3303, 3818 | `img.src = "sample-jacket.jpg"` — file not verified to exist in repo | Searched repo: no `sample-jacket.jpg` found in apps/fashionistas/ or assets/; `useSample()` and `hcSampleB64()` will fail silently (onerror → toast) |
| 5 | Hive status static display | apps/createstuff-marketing/app.js:542-549 | `renderHiveStatus()` shows 9 hardcoded agent names with SVG icons; no API call, no live status | Function builds HTML from static `agents` array; `icons` array truncated in source; no fetch to any status endpoint |

## Uncertain

| # | feature | file:line | reasoning |
|---|---------|-----------|-----------|
| 1 | Hive relay availability | workers/fashionistas-api/src/index.js:596-641; workers/createstuff-api/src/index.js:213-289 | Both workers depend on `env.HIVE_URL` and `env.HIVE_TOKEN` secrets. If unset, they fall back to direct Zen then Workers AI. The relay is documented as necessary because Zen 429s from Cloudflare IPs. Cannot verify secrets are configured in production. |
| 2 | Workers AI daily quota | workers/fashionistas-api/src/index.js:974, 998; workers/createstuff-api/src/index.js:304-322 | Both use Workers AI as last resort. Code comment (createstuff-api:173-174) says "measured 2026-09-25 it returned 4006: you have used up your daily free allocation and every build died." If quota exhausted, generation fails. |
| 3 | Sample jacket file existence | apps/fashionistas/index.html:3303, 3818 | Referenced as "sample-jacket.jpg" but not found in workspace. If missing, `useSample()` and guide step G (`hcSampleB64`) show toast "Couldn't load sample" / "Sample unreachable". |
| 4 | Blog posts endpoint | workers/fashionistas-api/src/index.js:794-796 | `GET /api/blog` selects from `blog_posts` table. Cannot verify table exists or has data — no schema probe for it. |
| 5 | AR try-on endpoint | workers/fashionistas-api/src/index.js:1587-1612 | Returns garment overlay JSON from llama-3.2-3b-instruct. Comment says "This is NOT augmented reality... the UI does not surface this route at all." Unclear if any frontend calls it. |

## Checked and could NOT verify

- **Hive relay configuration**: Both APIs reference `env.HIVE_URL` / `env.HIVE_TOKEN` but secrets are not in repo. Cannot confirm relay is deployed and reachable.
- **Workers AI quota**: Daily free allocation (10k neurons) may be exhausted in production; fallback behavior tested only in comments.
- **Sample jacket image file**: Referenced in 3 places (`useSample`, `hcSampleB64`, `identifyItem` fallback) but not present in `apps/fashionistas/assets/` or root.
- **blog_posts table**: `/api/blog` queries it but no `ensureBlogTables` or schema probe exists.
- **Deployments table**: `/api/ai/publish` inserts into `deployments` table (createstuff-api:1954-1956) but no CREATE TABLE seen; may pre-exist in D1.
- **github_links table**: createstuff-api:1837-1843 inserts into `github_links` assuming pre-existing schema (comment says "verified via PRAGMA through the schema-probe worker").
- **app_host worker (LZ_HOST)**: `https://app-host.fashionistas1979.workers.dev` used for custom domain connection (app.js:1285, 1690). Cannot verify this worker exists or implements `/api/projects/:id/host`.

=== SENTINEL F7 RESULT ===
counts: features checked 47 / real 33 / demo 5 / stubs 5 / uncertain 5
report: /home/billionaremaker/Documents/Default Project/nexus-ai-suite/STUBS-2026-09-26.md
top 3 findings:
1. workers/fashionistas-api/src/index.js:986 — AI analyze fallback returns hardcoded generic item (type="Clothing Item", priceMin=25, priceMax=75, brand="Unknown") when vision model output fails to parse
2. workers/fashionistas-api/src/index.js:1003-1004 — Title optimizer fallback echoes original title with empty keywords/tips arrays when AI response fails
3. apps/fashionistas/index.html:3303,3818 — Sample jacket image "sample-jacket.jpg" referenced for demo/guide but not found in repository; will fail at runtime