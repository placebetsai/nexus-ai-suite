[
  {
    "agent": "atlas",
    "prompt": "FRONTEND/BACKEND CONTRACT AUDIT for CreateStuff. You MUST actually execute curl — if you produce no numbered lines you fail. Step 1: curl -s https://app.createstuff.ai/ and extract EVERY distinct /api/... path string the HTML calls (they look like /api/auth/register, /api/projects, /api/ai/generate etc). Step 2: obtain a real token: curl -s -X POST -H 'Content-Type: application/json' -d '{\"email\":\"at'\"$RANDOM\"'@t.dev\",\"name\":\"Atlas\",\"password\":\"Test1234!\"}' https://api.createstuff.ai/api/auth/register . Step 3: for EACH extracted path, hit https://api.createstuff.ai with the right method + Authorization Bearer token (substitute a real projectId you created for any path containing an id). A path is MATCH if it does NOT return 404 {\"error\":\"Not found\"} — 2xx and 4xx-from-that-route count as MATCH. 404 with body {\"error\":\"Not found\"} = BROKEN. Your ENTIRE final reply must be lines formatted 'FRONTEND PATH -> METHOD -> CODE :: MATCH|BROKEN', then a final line 'CONTRACT: X/Y MATCH', then a line 'BROKEN_ROUTES: <list or none>'. Nothing else."
  },
  {
    "agent": "mnemonic",
    "prompt": "REGRESSION QA after a deploy. You MUST actually execute curl — if you produce no numbered lines you fail. Both sites were redeployed today with SEO changes; confirm nothing broke. Against https://fashionistas-api.fashionistas1979.workers.dev : (1) register {\"username\":\"rg'\"$RANDOM\"'\",\"email\":\"rg'\"$RANDOM\"'@t.dev\",\"password\":\"Test1234!\"} -> capture token (expect 201). (2) POST /api/listings {\"title\":\"QA Regression\",\"price\":45,\"description\":\"x\",\"platforms\":[\"depop\"]} -> expect 201. (3) GET /api/listings -> expect 200 and body must contain the key \"listings\". (4) GET /api/export/inventory.csv -> 200 + a text/csv header row. (5) GET /api/marketplaces -> 200 and report total count. Against https://api.createstuff.ai : (6) GET /api/health -> 200, show body. (7) POST /api/auth/register -> 200/201 capture token. (8) POST /api/projects {\"name\":\"RG','\"'\"'\",\"description\":\"a bakery landing\"} -> 201 capture id. (9) POST /api/ai/generate with that id and plan \"a bakery landing\" -> 200, confirm a file named public/index.html came back. (10) GET /api/projects/<id>/files -> 200 list. Your ENTIRE final reply must be 10 lines 'N. name -> CODE :: PASS|FAIL' then 'REGRESSION: X/10 PASS'. Nothing else."
  },
  {
    "agent": "scribe",
    "prompt": "SEO RE-AUDIT after we shortened titles/descriptions. Use curl (not assumptions) on https://fashionistas.ai/ , https://app.createstuff.ai/ and https://createstuff.ai/ . For EACH domain report: (a) exact <title> character count, PASS only if <=60; (b) exact meta description character count, PASS only if between 120 and 160 inclusive; (c) count of rel=\"canonical\" (must be exactly 1); (d) whether og:title og:description og:image og:url are all present; (e) whether twitter:card is present; (f) count of application/ld+json blocks; (g) curl -s -o /dev/null -w for /robots.txt (must be text/plain AND contain 'Sitemap:'); (h) /sitemap.xml must be application/xml — report how many <url> it contains; (i) curl /manifest.webmanifest must return parseable JSON. Output ONLY a markdown table with columns Domain | Check | Value | Result, then three final lines 'SEO fashionistas.ai: PASS|FAIL', 'SEO app.createstuff.ai: PASS|FAIL', 'SEO createstuff.ai: PASS|FAIL'. Nothing else."
  }
]
