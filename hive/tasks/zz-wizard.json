[
  {
    "agent": "vogue",
    "prompt": "You own apps/fashionistas/index.html (Fashionistas.ai — mobile-first AI resale SPA, ~858 lines, vanilla JS, real prod API api.fashionistas-ai.fashionistas1979.workers.dev verified live). ADD a guided A-to-Z popup-dialog walkthrough called the Mode Guide: a phone-sized little popup card sequence that walks a brand-new user from zero to first live crosslisted sale. HARD RULES: (1) Reuse the existing .sheet/.card/.chip/.btn/.tag styles already in the file — do NOT import any CSS framework and do NOT change :root variables. (2) A small floating circular '?' guide button (fixed, above the tabbar) toggles the sequence. (3) Steps cover: welcome(mention FREE + bring-your-own-AI-key optional) -> sign up (reuse the REAL doSignup/doLogin) -> snap a real photo (reuse the real camera/upload flow + real AI identifyItem) -> AI suggests title+price (real, source:ai) -> CROSS-LIST GUIDE: a checklist that walks the seller through setting up accounts on each marketplace (Depop, eBay, Poshmark, Mercari, Vinted, Grailed — your real marketplace list) with per-platform: open an account, set up payout, enable cross-listing in Fashionistas, done; every platform shows NEAR-PREBUILT copy-paste header/title/description ready to paste, NO external API keys required -> publish/cross-post (reuse the REAL crosspost(id)) -> mark sold (reuse the REAL markSold via soldSheet) -> see your profit (reuse renderHome analytics). (4) Each Next button must call an EXISTING function (doSignup, doLogin, identifyItem, crosspost, markSold, go) or a NEW function YOU define in the same edit; every new handler must be defined. (5) Do NOT break existing functions — no rename, no delete, no move. (6) Verify before finishing: extract the inline <script> to a temp .js file and run `node --check` on it; confirm no duplicate ids and that all onclick handlers exist. Output: exact lines added + node --check result + PHONE-OK if the 360px mobile layout + tabbar are intact."
  },
  {
    "agent": "forge",
    "prompt": "You own apps/createstuff/index.html (CreateStuff.ai — mobile-first free AI app builder SPA, ~520 lines, vanilla JS, real prod API api.createstuff.ai verified live). GOAL, non-negotiable: make CreateStuff genuinely vibe-codable from a PHONE — create, rebuild, edit, maintain, and publish websites from 360px mobile in a browser with zero card. HARD RULES: (1) Audit + fix the mobile layout: viewport meta, tap targets >=44px, the prompt input, template chips, file tabs, preview iframe, publish button, and project cards must all be usable at 360px wide on a phone with the bottom tabbar thumb-reachable. (2) ADD a guided 'How it works — A to Z' popup-dialog sequence (reuse existing inline styles / .card patterns, no new framework): steps = welcome(free + bring-your-own-AI-key optional) -> sign up (reuse REAL register/login) -> new app / describe an idea (real) -> AI generates the website (REAL ~35s via api/ai/generate) -> live preview -> PUBLISH to a real public URL (reuse the real publish flow) -> export / keep improving. (3) ADD a compact BYOK (bring-your-own-key) settings panel: user can paste any OpenAI-compatible API key (Gemini/Groq/OpenRouter/local Ollama) OR use the built-in free stack; toggle stored in localStorage; show a FREE badge when on the free path. (4) Publish must use the REAL api/ai/publish call already in the file — do not stub. (5) Do NOT break existing functions — no rename/delete/move. Verify: extract inline <script> and run `node --check`; confirm all onclick handlers defined. Output: exact lines changed + node --check result + PHONE-OK if the 360px layout passes."
  }
]
