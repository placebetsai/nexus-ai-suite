[
  {
    "agent": "mnemonic",
    "prompt": "You are QA. Run REAL curl tests against https://fashionistas-api.fashionistas1979.workers.dev — do not guess, actually execute. Steps: (1) POST /api/auth/register with body {\"username\":\"qa<random10>\",\"email\":\"qa<random10>@t.dev\",\"password\":\"Test1234!\"} and capture token. (2) POST /api/listings with Authorization Bearer token and body {\"title\":\"QA Jacket\",\"price\":45,\"description\":\"x\",\"platforms\":[\"depop\",\"ebay\"]} — expect 201. (3) POST /api/listings with {\"title\":\"no price\"} and NO price — expect 400 not 500. (4) GET /api/listings — expect 200 array. (5) PUT the created listing id. (6) DELETE it. (7) GET /api/export/inventory.csv — expect 200 and a header row. (8) GET /api/analytics — expect 200. Output ONLY a numbered list of 'METHOD /path -> actual HTTP CODE :: PASS|FAIL' plus a final line 'SUITE: X/Y PASS'. No prose."
  },
  {
    "agent": "sentinel",
    "prompt": "You are a security auditor. Run REAL curl tests against https://fashionistas-api.fashionistas1979.workers.dev — actually execute them. Test: (1) GET /api/listings with NO Authorization header -> expect 401. (2) GET /api/listings with Authorization: Bearer garbage -> expect 401. (3) GET /api/export/inventory.csv with no auth -> expect 401. (4) POST /api/auth/register with {\"username\":\"ab\",\"email\":\"x@y.z\",\"password\":\"123\"} -> expect 400 (validation). (5) POST /api/listings with title \"'; DROP TABLE listings; --\" and a token -> expect 201 or 400 but NEVER 500, and afterwards GET /api/listings must still 200 (table survived). (6) GET /api/health unauthenticated -> expect 200. Output ONLY 'N. name -> CODE :: PASS|FAIL' lines then 'SECURITY: X/Y PASS'. No prose."
  },
  {
    "agent": "vogue",
    "prompt": "You are the Fashionistas feature specialist. Run REAL curl tests against https://fashionistas-api.fashionistas1979.workers.dev — actually execute them. First register: POST /api/auth/register {\"username\":\"vg<random10>\",\"email\":\"vg<random10>@t.dev\",\"password\":\"Test1234!\"} -> token. Then test: (1) GET /api/marketplaces -> expect 200 and report EXACTLY how many marketplaces and how many have api='full'. (2) POST /api/ai/analyze {\"image\":\"<a real base64 JPEG of at least 300 chars>\"} -> expect 200 with a source field. (3) POST /api/listings/suggest-price {\"category\":\"Dress\",\"brand\":\"Zara\",\"condition\":\"Good\"} -> 200. (4) POST /api/ai/title-optimizer {\"title\":\"blue dress\",\"category\":\"Dress\"} -> 200. (5) POST /api/fees/estimate {\"price\":50,\"platform\":\"poshmark\"} -> 200, and verify net = 50 - fee - processing. (6) POST /api/shipping/estimate {\"weightOz\":12,\"carrier\":\"usps\"} -> 200. (7) POST /api/ai/tryon {\"garment_name\":\"denim jacket\"} -> report the ACTUAL code and body. Output ONLY 'N. name -> CODE :: PASS|FAIL' then 'FEATURES: X/Y PASS'. No prose."
  },
  {
    "agent": "forge",
    "prompt": "You are the CreateStuff backend tester. Run REAL curl tests against https://api.createstuff.ai — actually execute them, do not assume. Steps: (1) GET /api/health -> expect 200. (2) POST /api/auth/register {\"email\":\"fg<random10>@t.dev\",\"name\":\"Forge QA\",\"password\":\"Test1234!\"} -> token. (3) POST /api/projects {\"name\":\"QA Build\",\"description\":\"a landing page for a bakery\"} -> projectId. (4) POST /api/ai/generate {\"projectId\":\"<id>\",\"plan\":\"a landing page for a bakery\"} -> expect 200 and report whether real file contents came back. (5) GET /api/projects/<id>/files -> expect 200 with file list. (6) POST /api/ai/modify {\"projectId\":\"<id>\",\"message\":\"add a red header bar\"} -> report actual code. (7) POST /api/ai/publish {\"projectId\":\"<id>\"} -> report actual code and any URL. (8) POST /api/projects/import-github {\"repoUrl\":\"https://github.com/placebetsai/fashionistas-ai\"} -> expect 201. (9) GET /api/hive/capabilities -> expect 200, report how many models. Output ONLY 'N. name -> CODE :: PASS|FAIL' then 'CREATESTUFF: X/Y PASS'. No prose."
  },
  {
    "agent": "atlas",
    "prompt": "You are a frontend/backend contract auditor. First fetch https://fashionistas.ai/ and extract EVERY distinct /api/... path string the HTML calls. Then, for EACH extracted path, run the correct HTTP method against https://fashionistas-api.fashionistas1979.workers.dev with a real token you obtain via POST /api/auth/register {\"username\":\"at<random10>\",\"email\":\"at<random10>@t.dev\",\"password\":\"Test1234!\"}. Classify each as: EXISTS (2xx/4xx from that route) or MISSING (404 Not found = route absent). Do not test paths that clearly need an id with a literal value — substitute a real id you created. Output ONLY lines 'FRONTEND PATH -> METHOD -> CODE :: MATCH|BROKEN' then 'CONTRACT: X/Y MATCH'. Flag any BROKEN loudly. No prose."
  },
  {
    "agent": "ledger",
    "prompt": "You verify fee math with REAL calls against https://fashionistas-api.fashionistas1979.workers.dev. Register first: POST /api/auth/register {\"username\":\"ld<random10>\",\"email\":\"ld<random10>@t.dev\",\"password\":\"Test1234!\"} -> token. (1) GET /api/marketplaces, capture every marketplace id + feePct. (2) For EACH marketplace id, POST /api/fees/estimate {\"price\":100,\"platform\":\"<id>\"} with the token. (3) Verify arithmetic: fee == round(100*feePct/100, 2), processing == round(0.3+100*0.029,2)=3.2, net == 100-fee-processing. Flag ANY marketplace where the math fails or where feePct looks impossible (over 30%). Output ONLY '<id> feePct=<> net=<> :: PASS|FAIL' then 'FEES: X/Y PASS' plus a final line 'ANOMALIES: <comma list or none>'. No prose."
  },
  {
    "agent": "scribe",
    "prompt": "You are doing a live SEO audit. Use curl (not assumptions) on BOTH https://fashionistas.ai/ and https://app.createstuff.ai/. For each domain check: (1) <title> length <= 60 chars and unique vs the other domain. (2) meta description present, 120-160 chars. (3) exactly one rel=canonical. (4) og:title, og:description, og:image, og:url all present. (5) twitter:card present. (6) count of application/ld+json blocks. (7) curl /robots.txt returns text/plain and contains Sitemap:. (8) curl /sitemap.xml returns application/xml and count its <url> entries. (9) curl /manifest.webmanifest returns JSON. Then report a markdown table: Domain | Check | Result. Finish with lines 'SEO-A: PASS|FAIL' and 'SEO-B: PASS|FAIL'. Be exact with character counts."
  },
  {
    "agent": "pictor",
    "prompt": "You verify brand assets are actually served. Use curl on https://fashionistas.ai/ and https://app.createstuff.ai/. For each: (1) curl -o /dev/null -w '%{http_code} %{content_type}' /icon.svg — must be 200 image/svg+xml. (2) /manifest.webmanifest — 200, and validate the JSON parses; report name, theme_color, and icon src. (3) Extract og:image URL from the HTML and curl it — report ACTUAL http code and content type (if it 404s, say so loudly). (4) Check the HTML has zero references to files that are missing: extract href/src of local assets (starting with /) and curl each. Output ONLY 'N. asset -> CODE TYPE :: PASS|FAIL' then 'ASSETS: X/Y PASS' and a line 'OG_IMAGE_BROKEN: yes|no'. No prose."
  },
  {
    "agent": "nexus",
    "prompt": "You are verifying production uptime of the whole federation. Run curl -o /dev/null -w '%{http_code}' on EVERY one of these URLs and report each actual code: https://fashionistas.ai/ , https://fashionistas-ai.pages.dev/ , https://fashionistas.ai/robots.txt , https://fashionistas.ai/sitemap.xml , https://fashionistas.ai/icon.svg , https://fashionistas.ai/manifest.webmanifest , https://fashionistas-api.fashionistas1979.workers.dev/api/health , https://app.createstuff.ai/ , https://createstuff-app.pages.dev/ , https://createstuff.ai/ , https://createstuff.ai/robots.txt , https://createstuff.ai/sitemap.xml , https://app.createstuff.ai/robots.txt , https://app.createstuff.ai/sitemap.xml , https://api.createstuff.ai/api/health , https://forge-api.placebetsai.workers.dev/api/health . Then output ONLY 'CODE URL' lines sorted by code ascending, and finish with 'UPTIME: <count>/16 returned 200' and 'NOT_200: <list or none>'. No prose."
  },
  {
    "agent": "curator",
    "prompt": "You are the memory keeper. First, actually probe these with curl: https://fashionistas-api.fashionistas1979.workers.dev/api/health and https://api.createstuff.ai/api/health — record their exact JSON bodies. Then write a compact handoff markdown document to the file /home/billionaremaker/Documents/Default\\ Project/nexus-ai-suite/hive/output/PRIMETIME-STATUS.md containing sections: # Live URLs, # Backend versions (exact JSON you observed), # What was verified today (date 2026-09-24), # Known gaps. Be factual only — include nothing you did not directly observe via curl. Finish your reply with the single line WROTE-FILE."
  }
]
